if (!require("pacman")) install.packages("pacman")
pacman::p_load(dplyr,
               ggplot2,
               here,
               purrr,
               dplyr,
               tidyr,
               readr,
               magrittr,
               lubridate, 
               stringr, 
               vroom,
               pbapply, 
               parallel, 
               forcats,
               tidytable, 
               caret, 
               tidyverse, 
               rpart, 
               pbmcapply,
               doParallel,
               parallel,
               foreach,
               future,
               patchwork)
